
- Chatbot.py handles math queries, reversing words and simle llm queries
- app.py is a streamlit app for the same that handles logging along with other features.
- app.log shows all the logging information.
